import sys
import uvicorn
from lawyertools.app import app


if __name__ == "__main__":
    if len(sys.argv) == 2:
        port = int(sys.argv[1])
    else:
        port = 8000
    uvicorn.run(app, host="127.0.0.1", port=port, log_level="info")